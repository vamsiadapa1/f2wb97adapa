extends layout

block content
  h1= title
  - var image_names = ["bala1", "bala2", "bala3", "bala4", "bala5"]
  - for(let i = 0; i < 3; i++) {
    - let number = Math.floor(Math.random() * 5)
        img(src=`/images/${image_names[number]}.jpg`, alt="different images every time", height="300px", width="300px")
  - }
